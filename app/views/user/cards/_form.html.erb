<%= form_for(user_card) do |f| %>
  <% if user_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_card.errors.count, "error") %> prohibited this user_card from being saved:</h2>

      <ul>
      <% user_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :card_it %>
    <%= f.number_field :card_it %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
